# Results index

This file intentionally contains no numerical results. Historical planning,
energy, smoke-scale, and debug summaries were removed because they are not
valid evidence for the submitted manuscript.

The sole numerical record for the submission is `verified_metrics.json`.
The manuscript (`endohjepa.tex`) reports only entries that remain valid in
that ledger, and `audit_contact_protocol.json` records prior contacts with the
action audit partition.
