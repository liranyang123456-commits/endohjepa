# Archived methods notes

This file previously contained superseded method notes and invalid discrete-MPC
results. It must not be cited, used to generate figures, or included in a
submission package.

The authoritative manuscript is `endohjepa.tex`; the numerical source of truth
is `verified_metrics.json`. The implementation and the scripts that generate
every manuscript figure are the reproducible method record.
